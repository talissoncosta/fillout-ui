import { Meta, ArgTypes, Canvas, Story } from '@storybook/addon-docs/blocks';
import { IconButton } from 'src/components/icon-button';
import IconButtonStories, {
    Default,
    GhostVariant,
    Shapes,
    Sizes,
    StandardVariant,
    WithTooltip
} from './IconButton.stories'

<Meta of={IconButtonStories} />

# IconButton

`IconButton` is a compact button used to display a single icon, typically for secondary actions. It supports an optional `tooltip` prop for additional context, shown on hover or focus.


---

## Default usage

Use `IconButton` to represent common UI actions like "info", "delete", or "edit" using icons only.

<Canvas of={Default} />

---

## With Tooltip

Add a tooltip for accessibility and clarity:

<Canvas of={WithTooltip} />

---
### Sizes

The `IconButton` comes in predefined sizes to fit various layout needs. You can control the size using the `size` prop.

<Canvas of={Sizes} />

### Shapes

Customize the `IconButton`'s visual appearance with different `shape` options, such as `rounded` (default) or `square`.

<Canvas of={Shapes} />


### Variants

The `IconButton` offers different visual `variant`s to suit various design contexts.

#### Standard Variant

The `standard` variant is the default, providing a solid background and border.

<Canvas of={StandardVariant} />


#### Ghost Variant

The `ghost` variant is transparent by default, making it ideal for less prominent actions or when a subtle interaction is desired. It reveals its background on hover.

<Canvas of={GhostVariant} />

## Props

<ArgTypes of={IconButton} />

---

## Accessibility

- Tooltip appears on hover and keyboard focus.
- Button element is focusable and uses semantic HTML.
- Icon should include an `aria-label` or visually hidden text if used without tooltip.

---

## Best Practices

- Use for icon-only actions
- Avoid using multiple `IconButton`s too close together
- Provide a `tooltip` or `aria-label` for accessibility
