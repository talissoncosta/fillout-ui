import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import * as colors from 'src/theme'

<Meta title="Foundations/Colors" />

# Color Theme

<ColorPalette>
   {Object.entries(colors).filter(([name]) => name.includes('color') || name.includes('shadow')).map(([name, color]) => (
     <ColorItem
       title={name}
       colors={{ [name] : color }}
     />
   ))}
</ColorPalette>
